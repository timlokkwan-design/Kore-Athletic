# Kore Athletic 更新（2026.07.19-236）

Redeploy Streamlit Cloud 後，手機請**硬重新整理**。

## 修復
- **KeyError** on 課表檢視：`render_option_chips` 第一次載入未寫入 `session_state` 就讀取
- 而家會先寫入預設專項，再 return

目前 `APP_VERSION`：**2026.07.19-236**
